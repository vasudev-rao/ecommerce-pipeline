name: ecommerce_pipeline
version: "1.0.0"
config-version: 2

profile: ecommerce_pipeline

model-paths: ["models"]
test-paths:  ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  ecommerce_pipeline:
    staging:
      +materialized: view
      +schema: staging
    mart:
      +materialized: table
      +schema: mart
